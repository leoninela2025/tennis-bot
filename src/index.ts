import 'dotenv/config';

async function main() {
  // Placeholder entry point: wiring for scheduler + automation will live here.
  console.log('Tennis bot placeholder running. Implement scheduler + automation.');
}

main().catch((err) => {
  console.error('Fatal error', err);
  process.exit(1);
});
